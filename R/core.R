# Recoding in a simpler way

#' Create set of model conditions
#'
#' Takes the data and returns all possible models for faMuSSE
#'
#' @param traits data.frame: binary traits with row.names for taxa
#' @return a data.frame of possible models
fm_models <- function(traits) {
  nchar <- ncol(traits)
  
}
